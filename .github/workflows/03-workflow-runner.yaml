name: 03 - workflow runner
on: push
jobs:
  ubuntu-echo:
    runs-on: ubuntu-latest
    steps:
      - name: show os
        run: echo "runner os $RUNNER_OS"
  windows-echo:
    runs-on: windows-latest
    steps:
      - name: show os
        shell: bash
        run: echo "runner os $RUNNER_OS"